/** Row counts of the tables the metrics endpoint reports. */
export type MetricsTableTotals = {
  domainsTotal: number
  ipAddressesTotal: number
  rawReportsTotal: number
  normalizedEventsTotal: number
}
