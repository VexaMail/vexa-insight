apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vexa.fullname" . }}-config
  labels:
    {{- include "vexa.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
