apiVersion: v1
kind: Service
metadata:
  name: vexa
  namespace: vexa
  labels:
    app.kubernetes.io/name: vexa-insight
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vexa-insight
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
