**/node_modules/**
**/.next/**
**/dist/**
**/build/**
**/coverage/**
**/.lighthouseci/**
pnpm-lock.yaml
# Helm chart templates embed Go template syntax (`{{ }}`) inside YAML;
# prettier inserts spaces inside the braces and breaks the chart.
deploy/helm/**
# K8s manifests are hand-formatted; keep prettier off.
deploy/k8s/**
# Drizzle's journal is autogenerated; keep its trailing-newline-free format.
drizzle/meta/_journal.json
# Git worktrees checked out inside the repo carry their own copy of the tree;
# eslint.config.ts ignores them for the same reason.
.worktrees/**
